@model Kooboo.CMS.Modules.Publishing.Models.RemotePublishingQueue
@using Kooboo.CMS.Modules.Publishing.Models;
@{
    ViewBag.Title = "Remote publsihing queue detail".Localize();
    Layout = "~/Views/Shared/Blank.cshtml";
}
@section Panel{
    <ul class="panel">
        <li>
            <a href="@ViewContext.RequestContext.GetRequestValue("return")">
                @Html.IconImage("cancel") @("Back".Localize())
            </a>
        </li>
    </ul>
}
<div class="common-form">
    <h1 class="title">@ViewBag.Title</h1>
    @using (Html.BeginForm())
    {
        <table>
            <tbody>
                @Html.DisplayFor(m => m.PublishingObject)
                @Html.DisplayFor(m => m.ObjectUUID)
                @Html.DisplayFor(m => m.ObjectTitle)
                @Html.DisplayFor(m => m.UtcTimeToPublish)
                @Html.DisplayFor(m => m.UtcTimeToUnpublish)
                @Html.DisplayFor(m => m.UtcProcessedPublishTime)
                @Html.DisplayFor(m => m.UtcProcessedUnpublishTime)
                @if (Model.PublishingObject == PublishingObject.Page)
                {
                    @Html.DisplayFor(m => m.RemoteEndpoint)
                    //   @Html.DisplayFor(m=>m.PublishDraft)
                }
                @if (Model.PublishingObject == PublishingObject.TextContent)
                {
                    @Html.DisplayFor(m => m.TextFolderMapping)
                }
                @Html.DisplayFor(m => m.Status)
                @Html.DisplayFor(m => m.UserId)
                @Html.DisplayFor(m => m.UtcCreationDate)
                @Html.DisplayFor(m => m.Message)
            </tbody>
        </table>
    }
</div>
