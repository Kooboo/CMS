@model Kooboo.CMS.Sites.Models.View
@{
var editorPath = (string)ViewBag.ViewEngine.EditorVirtualPath;
}
@if (editorPath == null)
{
    @Html.Partial("_ViewDefaultEditor", Model)
}
else
{
    @Html.Partial(editorPath, Model)
}