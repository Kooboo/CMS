<h3>Product</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["ProductId"], "Product/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>