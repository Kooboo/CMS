<h3>category</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["title"], "category/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>