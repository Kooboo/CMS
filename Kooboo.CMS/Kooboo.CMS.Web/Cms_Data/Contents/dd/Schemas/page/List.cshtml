<h3>page</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["name"], "page/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>