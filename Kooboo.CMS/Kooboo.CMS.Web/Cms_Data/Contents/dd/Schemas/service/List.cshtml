<h3>service</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["title"], "service/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>