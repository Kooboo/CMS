@model Kooboo.CMS.Web.Areas.Sites.Models.ImportRepositoryModel
@{
    ViewBag.Title = "Import database".Localize();
    Layout = "~/Views/Shared/Blank.cshtml";
}

@section Panel{
    <ul class="panel">
        <li>
            <a data-ajaxform="">
                @Html.IconImage("save") @("Save".Localize())</a>
        </li>
        <li>
            <a href="@ViewContext.RequestContext.GetRequestValue("return")">
                @Html.IconImage("cancel") @("Back".Localize())</a>
        </li>
    </ul>
}
<div class="block common-form">
    <h1 class="title">@ViewBag.Title @("for".Localize()) <strong>@Site.Current.FriendlyName</strong></h1>

    @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true)
        <table>
            @Html.EditorFor(o => o.Repository)
            @Html.EditorFor(o => o.File, new { @class = "long" })
            @Html.EditorFor(o => o.DeleteTheOldRepository)
        </table> 
    }
</div>
